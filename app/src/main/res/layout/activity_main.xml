<?xml version="1.0" encoding="UTF-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <data>
        <import type="com.noactivity.noactivitytest.converter.IntConverter" />
        <variable
            name="viewmodel"
            type="com.noactivity.noactivitytest.viewmodel.MainViewModel" />
        <variable name="tag" type="com.noactivity.noactivitytest.viewmodel.TagEnum" />
    </data>
    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".fragment.MainFragment"
        >
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            >
            <Button
                android:layout_width="200dp"
                android:layout_height="300dp"
                android:text="Tap Event"
                />
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Hello world3"
                />
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Hello world"
                />
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="number"
                android:text="@{IntConverter.intToString(viewmodel.counter)}"
                />
            <LinearLayout
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:orientation="horizontal"
                >
                <Button
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="+"
                    android:onClick="@{() -> viewmodel.OnButtonTapped(tag.Plus)}"
                    />
                <Button
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="-"
                    android:onClick="@{() -> viewmodel.OnButtonTapped(tag.Minus)}"
                    />
                <Button
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="コルーチン"
                    android:onClick="@{() -> viewmodel.OnCoroutine()}"
                    />
                <Button
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="円"
                    android:onClick="@{() -> viewmodel.OnDrawCircle()}"
                    />
                <Button
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="test"
                    android:onClick="@{() -> viewmodel.sendTap()}"
                    />
            </LinearLayout>
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{viewmodel.Message}"
                />
            <com.noactivity.noactivitytest.view.CustomCanvas
                android:layout_width="400dp"
                android:layout_height="400dp"
                android:background="#00FFFFFF"
                CenterX="@{viewmodel.CenterX}"
                CenterY="@{viewmodel.CenterY}"
                ColorA="@{viewmodel.ColorA}"
                ColorR="@{viewmodel.ColorR}"
                ColorG="@{viewmodel.ColorG}"
                ColorB="@{viewmodel.ColorB}"
                Radius="@{viewmodel.Radius}"
                Thickness="@{viewmodel.Thickness}"
                IsDraw="@{viewmodel.IsDraw}"
                />
        </LinearLayout>
    </androidx.appcompat.widget.LinearLayoutCompat></layout>
