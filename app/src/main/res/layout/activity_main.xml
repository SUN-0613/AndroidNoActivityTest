<?xml version="1.0" encoding="UTF-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <data>
        <import type="com.noactivity.noactivitytest.converter.IntConverter" />
        <variable
            name="viewmodel"
            type="com.noactivity.noactivitytest.viewmodel.MainViewModel" />
        <variable name="tag" type="com.noactivity.noactivitytest.viewmodel.TagEnum" />
    </data>
    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".fragment.MainFragment"
        >
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            >
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Hello world3"
                />
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Hello world"
                />
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="number"
                android:text="@{IntConverter.intToString(viewmodel.counter)}"
                />
            <LinearLayout
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:orientation="horizontal"
                >
                <Button
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="+"
                    android:onClick="@{() -> viewmodel.OnButtonTapped(tag.Plus)}"
                    />
                <Button
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="-"
                    android:onClick="@{() -> viewmodel.OnButtonTapped(tag.Minus)}"
                    />
                <Button
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="コルーチン"
                    android:onClick="@{() -> viewmodel.OnCoroutine()}"
                    />
                <Button
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="円"
                    android:onClick="@{() -> viewmodel.OnDrawCircle()}"
                    />
            </LinearLayout>
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Hello world2"
                />
            <ImageView
                android:layout_width="300dp"
                android:layout_height="300dp"
                android:scaleType="center"
                android:background="@color/white"
                android:src="@{viewmodel.circle}"
                />
            <!-- android:src="@drawable/my_ellipse" -->
        </LinearLayout>
    </androidx.appcompat.widget.LinearLayoutCompat></layout>
